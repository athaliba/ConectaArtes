// import { render, screen } from '@testing-library/react';
// import SearchComponent from '../../components/SearchComponent';

// describe('SearchComponent - renderização básica', () => {
//   test('renderiza título, input e botão', () => {
//     render(<SearchComponent userId="user123" token="token" />);
    
//     expect(screen.getByText(/Buscar Restaurantes por CEP/i)).toBeInTheDocument();
//     expect(screen.getByLabelText(/Digite o CEP/i)).toBeInTheDocument();
//     expect(screen.getByRole('button', { name: /Buscar Restaurantes/i })).toBeInTheDocument();
//   });
// });
